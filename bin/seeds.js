//IN this file, we'll create false  users, librarias and books that only w will be able to serve-favicon
/*jshint esversion:6*/
const mongoose = require("mongoose");
mongoose.connect("mongodb://localhost/pair-project");
const User = require('../models/user');


const users = [
  {
    name
  }
];
